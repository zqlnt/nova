import type { CalendarEvent, TermDate } from '@/lib/calendarTypes';

/** No demo events — calendar shows real data when you add sessions in Nova. */
export const studentCalendarEvents: CalendarEvent[] = [];

export const uk2026TermDates: TermDate[] = [];
